!! NOT ACTUALLY A USER MANUAL

- The INSERT key will toggle the insertion mode. While in the default mode (INSERT), when typing or pressing Backspace, the cursor will automatically move between cells. While in OVERWRITE mode, the cursor will not move between cells unless manually moved using the arrow keys or by clicking.